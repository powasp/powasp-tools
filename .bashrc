export PATH="$PATH:/powasp/bin"

# Source a user-specific bash profile
# Place profile at /powasp/.bash_profile and uncomment the following line:
[[ -f /powasp/.bash_profile ]] && source /powasp/.bash_profile
